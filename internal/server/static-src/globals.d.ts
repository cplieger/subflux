// Injected by cmd/bundle: the content-hashed /chunks/sse-worker-<hash>.js the
// page hands to `new SharedWorker`. worker-url-setup.ts installs a fixed one
// for the suites.
declare const __SSE_WORKER_URL__: string;
